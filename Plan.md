# План миграции CRM на локальную SQLite-базу

## Нужно сделать

- [ ] Отдельным этапом заменить `xlsx` или принять риск, потому что npm не предлагает исправления уязвимостей для текущего пакета.

## В работе

- [ ] Эксплуатация локальной версии v1.

## Выполнено
- [x] Добавлена единая логика автонумерации договоров по категории: `ЧЧ`, `ГБ`, `Б`, `Ф`.
- [x] При создании договора номер теперь автоматически формируется как `префикс + число`, например `ЧЧ112`, `ГБ54`, `Б7`, `Ф3`.
- [x] Автонумерация учитывает создание договора из предзаполненной брони бани/фурако: сервисные договоры получают префиксы `Б` и `Ф` без ручной правки.
- [x] Счет, акт и договор используют один и тот же `contract.number`, поэтому новый префикс автоматически попадает во все документы и email-темы.
- [x] PDFMe-акт приведен ближе к присланному образцу: `Акт № ... от date_out`, исполнитель/заказчик, таблица шириной как у счета, итоговые строки, сумма прописью и два блока подписи.
- [x] Исправлены поля акта в сохраненном `invoice_pdfme`: убраны имена с пробелом на конце, добавлены `act_qty_words`, `act_total_words_only`, `act_customer_title`, `act_client_name_short`.
- [x] Для акта НДС считается как `5%` от итога, чтобы совпадать с образцом акта.
- [x] Перед обновлением акта созданы бэкапы: `data/backups/before-pdfme-act-layout-reference-2026-04-21-02-32-38.sqlite` и `data/backups/before-pdfme-act-date-vat-signature-2026-04-21-02-35-15.sqlite`.
- [x] В PDFMe-счет добавлена вторая страница акта выполненных работ с той же A4-шириной и отступами `20/15`.
- [x] Сохраненный SQLite-шаблон `invoice_pdfme` обновлен без изменения первой страницы счета: было 1 страница, стало 2.
- [x] Перед обновлением PDFMe-шаблона создан бэкап базы: `data/backups/before-pdfme-invoice-act-page-2026-04-20-23-49-31.sqlite`, `PRAGMA integrity_check = ok`.
- [x] Для акта добавлены PDFMe-поля предпросмотра и переменные редактора: `act_header`, `act_client_name`, `act_service_row_*`.
- [x] Создана и проверена резервная копия базы после ручной настройки PDFMe-счета: `data/backups/after-pdfme-invoice-user-layout-2026-04-20-23-40-09.sqlite`, `PRAGMA integrity_check = ok`.
- [x] Пользовательский PDFMe-макет счета после ручного выравнивания сохранен в SQLite под стабильным ID `invoice_pdfme`.
- [x] Исправлена кнопка приближения PDFMe Designer: `maxZoom` заменен с `4` на `400`, потому что PDFMe ожидает значение в процентах.
- [x] В панели полей PDFMe очищены остатки битой кодировки в подсказках удаления и статусной строке.

- [x] Принят целевой вариант: один ПК, один менеджер, локальная SQLite-база.
- [x] Принят режим первого рабочего варианта: локальный сервер + браузер.
- [x] Принято хранение SMTP-пароля в `.env.local`.
- [x] Принято требование одноразового импорта из Firebase перед удалением Firebase.
- [x] Создан журнал `Plan.md`.
- [x] Добавлена SQLite-база `data/crm.sqlite` с автоинициализацией схемы.
- [x] Добавлены локальные API для клиентов, договоров, настроек, реквизитов, шаблонов, email и бэкапов.
- [x] Фронтенд переключен с прямых Firestore-вызовов на локальный API.
- [x] ID услуг унифицированы: `gb-bath` и `gb-furako`.
- [x] Добавлена серверная проверка пересечения броней.
- [x] SMTP-пароль больше не хранится во фронтенде и не передается с клиента.
- [x] Добавлен ручной бэкап SQLite и автобэкап перед импортом Firebase.
- [x] README обновлен под локальный режим.
- [x] Очищены остаточные фронтенд-заглушки после перехода на локальный API: мертвые `if (false)`, старый `snap.forEach`, клиентские типы `smtpSettings/appPassword`.
- [x] Добавлен повторяемый smoke-test локального API: `npm run test:local-api`.
- [x] Firebase-импорт выполнен на реальных данных: клиенты и договоры появились, договоры формируются.
- [x] Новый SMTP-пароль приложения внесен в `.env.local`.
- [x] Добавлена диагностика SMTP: `npm run smtp:check`.
- [x] Ручная приемка локальной версии на ПК менеджера выполнена: клиенты, договоры, формирование документов и email работают.
- [x] После успешного импорта удалены Firebase-конфиги, Firestore rules, импортный endpoint и зависимость `firebase`.
- [x] Выполнен `npm audit fix`: исправляемые уязвимости обновлены, остался только `xlsx`.
- [x] Доработано удобство работы с HTML-шаблонами: проверка переменных, вставка переменной в курсор, надежное сохранение изменений редактора.
- [x] Убран опасный fallback на чужой шаблон при формировании документов.
- [x] Добавлен серверный бэкап старой версии HTML-шаблона перед перезаписью.
- [x] Шаблон счёта `invoice` перенесён из `C:\Users\Make\Documents\Счет на оплату.docx` в чистый HTML без Word/Mso-стилей.
- [x] Исправлено падение сохранения HTML-шаблона, когда Jodit временно не отдаёт native editor value после ручных правок.
- [x] Шаблон акта `act` перенесён из `C:\Users\Make\Documents\Акт.docx` в чистый HTML.
- [x] Формирование счёта и акта объединено в один PDF-документ с разрывом страницы между счётом и актом.
- [x] Удалено нефункциональное поле "Быстрый поиск" из верхней панели.
- [x] Шаблон договора `cc_cottage` перенесён из `C:\Users\Make\Documents\договор чунга чанга.docx` в чистый HTML с восстановленной нумерацией пунктов и исправленными лишними пробелами.
- [x] Исправлен редактор HTML-шаблонов: восстановлен скроллинг длинных шаблонов и убран CSS-класс, который мешал снять жирный шрифт в шаблоне `cc_cottage`.
- [x] Доработан скроллинг редактора после проверки по скриншоту: колесо мыши над редактором принудительно прокручивает внутренний слой Jodit `.jodit-workplace`.
- [x] Найдена основная причина проблемы скроллинга Jodit: высота прокрутки считалась внутри ограниченного слоя редактора. Редактор переведён на внешний scroll-контейнер, а `.jodit-workplace` и `.jodit-wysiwyg` теперь растут по фактической высоте HTML-шаблона.
- [x] Исправлено центрирование листа в редакторе после перевода на внешний scroll-контейнер: контейнеры Jodit снова занимают всю ширину рабочей области, а лист центрируется через auto-margin.
- [x] Добавлены визуальные разрывы страниц в редактор HTML-шаблонов: пунктирная линия показывает конец каждой A4-страницы, но не сохраняется в HTML шаблона.
- [x] Шаблон договора `cc_cottage` повторно перенесён по новым эталонам `C:\Users\Make\Documents\Documents\договор чунга чанга.docx` и `C:\Users\Make\Documents\Documents\договор чунга чанга.pdf`: договор и допсоглашение разбиты на 4 A4-страницы.
- [x] Исправлен неудачный перенос `cc_cottage`: из HTML-шаблона убраны вложенные A4-страницы `data-document-page`, оставлены обычные разрывы `.document-page-break`, чтобы редактор не показывал "лист внутри листа".
- [x] В редактор HTML-шаблонов добавлены инструменты: вставить/удалить разрыв страницы, выравнивание текста в ячейках таблицы влево/центр/вправо и по вертикали вверх/середина/низ.
- [x] Добавлена загрузка DOCX-файла как содержимого выбранного HTML-шаблона: DOCX конвертируется в HTML, сохраняется в локальную базу и сразу открывается в редакторе для проверки.
- [x] В редактор HTML-шаблонов добавлен выбор межстрочного интервала, панель инструментов таблиц разбита на понятные группы, кастомные иконки заменены на более наглядные.
- [x] Добавлен отдельный HTML-шаблон `cc_addendum` (`Доп соглашение ЧЧ`) и автоматическое присоединение его страниц после шаблона `Договор / Чунга-Чанга / Коттедж` при формировании договора и PDF/email.

## Проверки

- [x] `npm run lint`
- [x] `npm run build`
- [x] `npm run test:local-api`
- [x] Проверено отсутствие остаточных клиентских `if (false)`, `smtpSettings`, `appPassword`, `snap.forEach`.
- [x] Smoke-test локального сервера: `GET /api/health`.
- [x] Проверено, что `GET /api/email-settings` не возвращает `appPassword`.
- [x] Проверено создание резервной копии через `POST /api/backups`.
- [x] API-проверка CRUD клиента, создания договора/предброни и отказа пересекающейся брони.
- [x] Проверено, что временные `codex-test-*` записи удаляются после smoke-test.
- [x] Ручная проверка импорта Firebase.
- [x] Проверено чтение SMTP-переменных из `.env.local`.
- [x] Проверено, что с текущего ПК `smtp.yandex.ru:465` и `smtp.yandex.ru:587` недоступны по TCP.
- [x] `npm audit --audit-level=moderate` выполнен после `npm audit fix`; осталась известная уязвимость `xlsx` без npm-исправления.
- [x] Финальная проверка запуска после удаления Firebase-части.
- [x] Проверено, что генератор документов больше не использует fallback на чужой шаблон.
- [x] `npm run lint` после доработки шаблонов.
- [x] `npm run build` после доработки шаблонов.
- [x] `npm run test:local-api` после доработки шаблонов.
- [x] Исправлено растягивание таблиц в предпросмотре/PDF: убран принудительный `table width: 100%`, поля документа выровнены с редактором.
- [x] Унифицирована логика инструментов таблиц в редакторе: выделение строк/столбцов, скрытие/показ границ ячеек, текущей таблицы и всех таблиц.
- [x] Подстановка переменных в HTML теперь экранирует спецсимволы, чтобы данные клиента/услуги не ломали разметку таблиц.
- [x] Проверено, что новый HTML-шаблон `invoice` сохранён в SQLite, содержит нужные переменные и не содержит `MsoNormal`/`data-borders-hidden`.
- [x] Перед заменой `invoice` создан бэкап старого HTML-шаблона в `data/backups/templates`.
- [x] `npm run lint`, `npm run build`, `npm run test:local-api` после исправления сохранения Jodit.
- [x] Проверено, что новый HTML-шаблон `act` сохранён в SQLite и содержит обязательные переменные акта.
- [x] Проверено, что предпросмотр/PDF поддерживает несколько страниц для связки `invoice` + `act`.
- [x] `npm run lint` после удаления поля "Быстрый поиск".
- [x] Проверено, что новый HTML-шаблон `cc_cottage` сохранён в SQLite, содержит обязательные переменные договора и не содержит Word/Mso-разметку.
- [x] Проверено, что шаблон `cc_cottage` больше не содержит класс `cc-bold`; жирный текст хранится через `<strong>`.
- [x] `npm run lint` и `npm run build` после исправления скроллинга редактора шаблонов.
- [x] `npm run lint` и `npm run build` после второй правки скроллинга Jodit.
- [x] `npm run lint` и `npm run build` после перевода редактора шаблонов на внешний scroll-контейнер.
- [x] `npm run lint` и `npm run build` после исправления центрирования листа в редакторе.
- [x] `npm run lint` и `npm run build` после добавления визуальных разрывов страниц в редакторе шаблонов.
- [x] Проверено, что новый `cc_cottage` содержит 4 страницы, не содержит неизвестных переменных и не содержит старый захардкоженный номер `ЧЧ216067`.
- [x] Визуально проверен PNG-рендер нового `cc_cottage` по 4 страницам.
- [x] `npm run lint` и `npm run build` после повторного переноса шаблона `cc_cottage`.
- [x] Проверено, что исправленный `cc_cottage` больше не содержит `data-document-page`, содержит 3 маркера `.document-page-break`, неизвестных переменных нет.
- [x] Визуально проверен PNG-рендер исправленного `cc_cottage` по 4 страницам.
- [x] `npm run lint` и `npm run build` после исправления вложенных A4-страниц в `cc_cottage`.
- [x] `npm run lint` и `npm run build` после добавления инструментов разрыва страницы и выравнивания ячеек таблицы.
- [x] `npm run lint` после добавления DOCX-загрузки шаблонов.
- [x] `npm run build` после добавления DOCX-загрузки шаблонов.
- [x] `npm run lint` после добавления межстрочного интервала и обновления иконок редактора.
- [x] `npm run build` после добавления межстрочного интервала и обновления иконок редактора.
- [x] `npm run lint` после добавления шаблона `Доп соглашение ЧЧ`.
- [x] `npm run build` после добавления шаблона `Доп соглашение ЧЧ`.
- [x] Проверено, что `cc_addendum` сохранён в SQLite, содержит переменные `{contract_number}`, `{sign_date}`, `{client_name_gen}`, `{client_name_short}`, `{date_out}` и один внутренний разрыв страницы.
- [x] Ручная проверка CRUD клиентов.
- [x] Ручная проверка CRUD договоров и предброней.
- [x] Ручная проверка конфликта пересекающихся броней.
- [x] Ручная проверка отправки email через `.env.local`.
- [x] Ручная проверка создания резервной копии.

## Заметки

- Пароль приложения Яндекса, который был в исходном коде, нужно считать скомпрометированным и перевыпустить.
- Единые ID услуг для Голубой Бухты: `gb-bath` и `gb-furako`.
- База v1: `data/crm.sqlite`.
- Бэкапы v1: `data/backups`.
- Firebase-файлы удалены после успешного одноразового импорта и ручной проверки.
- Для `xlsx` npm сейчас не предлагает исправления; пакет нужен для Excel-отчетов шахматки, замену лучше планировать отдельным этапом.
- Ошибка email `connect ECONNREFUSED 77.88.21.158:465` указывала на отказ TCP-подключения к SMTP, а не на ошибку пароля. Пользователь подтвердил, что причиной был VPN.
- После отключения VPN письмо успешно отправилось из CRM.
- На момент проверки в SQLite настроены шаблоны `gb_cottage`, `cc_cottage`, `invoice` и `act`; остальные карточки честно показываются как "Не настроен" и требуют заполнения перед формированием соответствующих документов.

## Аудит кода 2026-04-18

### Выполнено

- [x] Добавлена нормализация переменных шаблонов перед проверкой, сохранением и генерацией: переменные вида `{ contract_number }` или разбитые HTML-тегами после DOCX-импорта приводятся к `{contract_number}`.
- [x] Генерация документов теперь останавливается, если в шаблоне есть неизвестные или обязательные отсутствующие переменные, вместо молчаливой генерации битого договора.
- [x] Шаблон `cc_cottage` в SQLite заменен на чистый HTML без `crm-docx-import` и вложенных `data-document-page`; договор оставлен на 2 страницах, а допсоглашение продолжает прикрепляться отдельно при формировании.
- [x] В шаблоне `cc_cottage` исправлена нумерация пунктов `3.12` / `3.13` / `3.14`, из-за которой список визуально сбивался.
- [x] Перед заменой `cc_cottage` создан бэкап старого HTML-шаблона в `data/backups/templates`.
- [x] Ошибки API при сохранении клиентов/договоров/SMTP теперь показываются пользователю текстом от сервера, включая конфликт пересекающихся броней.
- [x] SMTP-ошибки `/api/send-email` снова проходят через понятный перевод сетевых/авторизационных ошибок.
- [x] Из клиентского кода убраны отладочные `console.log` с договорами, бронями и длиной HTML-документа.
- [x] HTML письма экранирует имя клиента, тип документа, номер договора, пользовательское сообщение и подпись отправителя.
- [x] `localApi` устойчивее обрабатывает не-JSON ответы сервера и не теряет текст ошибки.
- [x] В редакторе HTML-шаблонов увеличены иконки панели Jodit и кликабельная область кнопок; панель переменных свернута вправо и открывается отдельной кнопкой "Переменные".
- [x] В редактор HTML-шаблонов добавлен масштаб страницы документа: 75%, 90%, 100%, 110%, 125%, 150% с кнопками уменьшения/увеличения.
- [x] Исправлена защита черновиков редактора шаблонов: если локальный черновик свежее сохраненной версии, редактор открывает именно черновик и не затирает его серверной копией.
- [x] Служебные атрибуты выделения ячеек `data-jodit-selected-cell` больше не сохраняются в HTML-шаблон и очищаются перед черновиком/проверкой/сохранением.
- [x] Запись черновиков шаблонов в `localStorage` переведена на debounce и защищена от ошибок переполнения локального хранилища.
- [x] Масштаб страницы в редакторе переведен с CSS `zoom` на физическое изменение ширины, высоты и полей A4-листа, чтобы текст не размывался из-за `transform: scale(...)`.
- [x] `{exec_name}` убран из списка обязательных переменных шаблонов; исполнитель остался доступной переменной для ручного использования, но больше не блокирует сохранение.
- [x] В шаблоне `cc_cottage` ослаблены слишком плотные интервалы: увеличены `line-height`, отступы между пунктами, отступы заголовков разделов и ширина колонки номера пункта.
- [x] Перед правкой интервалов `cc_cottage` создан бэкап старого HTML-шаблона в `data/backups/templates`.
- [x] Внедрен первый этап `pdfme` параллельно текущим HTML-шаблонам: добавлены зависимости `@pdfme/common`, `@pdfme/generator`, `@pdfme/schemas`, `@pdfme/ui`.
- [x] Добавлена SQLite-таблица `pdf_templates`, локальные API `/api/pdf-templates` и резервное сохранение старых PDFMe JSON-шаблонов в `data/backups/templates`.
- [x] Добавлен базовый PDFMe-шаблон счета `invoice_pdfme` с координатными полями и динамической таблицей услуг.
- [x] Добавлен редактор PDFMe-шаблона счета через `Designer` из `@pdfme/ui`; редактор загружается лениво, чтобы не раздувать основной JS-бандл.
- [x] В карточку договора добавлена генерация `PDFMe: Счёт`; старые HTML/PDF документы остаются рабочими.
- [x] Предпросмотр документов теперь умеет открывать готовый PDF `Blob` в iframe и сохранять его без повторной HTML-конвертации.
- [x] Исправлены review-замечания по HTML-zoom: A4-разметка редактора снова фиксированная, масштаб применяется визуально, а направляющие страниц считаются от реальной A4-высоты.
- [x] Исправлены review-замечания по PDFMe: введен стабильный ID `invoice_pdfme`, добавлена миграция старых `invoice_pdfme_v*`, боковая панель больше не удаляет страницы после первой, генератор получает полный набор переменных, сброс шаблона помечается как несохраненное изменение, сохраненный шаблон больше не подменяется дефолтным из-за отсутствия таблицы.
- [x] Для PDFMe добавлен реальный bold-шрифт `public/fonts/NotoSerif-Bold.ttf`; старые `fontWeight: bold` нормализуются в `fontName: NotoSerifBold`.
- [x] Исправлен сохраненный PDFMe-шаблон `invoice_pdfme`: у таблицы услуг удалены битые `lineHeight: 0/0.1`, восстановлены высота 42 мм, ширины колонок, паддинги и выравнивания, чтобы длинное название услуги переносилось без наложения текста.
- [x] Таблица услуг в PDFMe-счете переведена с встроенного элемента `table` на ручную разметку из линий и текстовых полей. Это убирает наложение жирной горизонтальной линии на заголовки и строку услуги при генерации PDF.
- [x] Сохраненный SQLite-шаблон `invoice_pdfme` обновлен до ручной таблицы услуг; генератор и предпросмотр получают новые поля `service_row_*`.
- [x] В PDFMe Designer подключен плагин `Multi-Variable Text`; предпросмотр и генератор теперь передают ему JSON-значения переменных из данных CRM.
- [x] В PDFMe Designer подключены расширенные схемы `Image`, `Signature`, `SVG`, `QR`; предпросмотр и генератор корректно обрабатывают пустые image/signature/svg-поля, чтобы плейсхолдеры печати/подписи не ломали PDF.
- [x] В PDFMe Designer добавлена вкладка `Блоки` и повторяемый блок `Реквизиты + подписи + печать`: контейнер вставляется на текущую страницу как набор связанных объектов с общим `componentId`, Multi-Variable Text для реквизитов и Image-плейсхолдером под штамп.

### Проверки

- [x] `npm run lint`
- [x] `npm run build`
- [x] `npm run test:local-api`
- [x] `npm audit --audit-level=moderate` выполнен; `protobufjs` исправлен через `npm audit fix`, осталась известная проблема `xlsx` без доступного npm-исправления.
- [x] `npm run lint` после доработки иконок и панели переменных.
- [x] `npm run build` после доработки иконок и панели переменных.
- [x] `npm run lint` после добавления масштаба страницы документа.
- [x] `npm run build` после добавления масштаба страницы документа.
- [x] `npm run lint` после исправления замечаний по редактору шаблонов.
- [x] `npm run build` после исправления замечаний по редактору шаблонов.
- [x] `npm run lint` после удаления `{exec_name}` из обязательных переменных.
- [x] `npm run build` после удаления `{exec_name}` из обязательных переменных.
- [x] `npm run lint` после исправления размытия текста и интервалов в `cc_cottage`.
- [x] `npm run build` после исправления размытия текста и интервалов в `cc_cottage`.
- [x] Повторно проверено review-замечание по масштабу редактора: `CSS zoom` в `HtmlTemplateEditorModal.tsx` отсутствует, масштаб работает через размеры A4-листа; `npm run lint` и `npm run build` проходят.
- [x] `npm run lint` после внедрения первого этапа PDFMe.
- [x] `npm run build` после внедрения первого этапа PDFMe; PDFMe вынесен в lazy chunks.
- [x] Сервер CRM перезапущен; `GET /api/health` возвращает `{"ok":true}`.
- [x] Проверено, что таблица `pdf_templates` создана, а шаблон `invoice_pdfme` сохранен в SQLite.
- [x] Smoke-тест PDFMe: создан `tmp/pdfs/pdfme-invoice-smoke.pdf`, кириллица извлекается корректно, текстовые блоки не выходят за границы страницы.
- [x] `npm run lint` после исправления review-замечаний по HTML-zoom и PDFMe.
- [x] `npm run build` после исправления review-замечаний по HTML-zoom и PDFMe.
- [x] `npm run test:local-api` после запуска CRM-сервера на `localhost:3000`.
- [x] Проверено, что стабильный PDFMe-шаблон `invoice_pdfme` создан в SQLite миграцией из `invoice_pdfme_v9`.
- [x] Smoke-тест PDFMe после добавления bold-шрифта: создан `tmp/pdfs/pdfme-default-test.pdf`, кириллица извлекается корректно, подписи и итоговые строки читаются.
- [x] Smoke-тест сохраненного PDFMe-шаблона после ремонта таблицы: создан `tmp/pdfs/pdfme-saved-template-test.pdf`, документ остался на 1 странице, таблица услуг и итоговые строки извлекаются в правильном порядке.
- [x] Smoke-тест ручной PDFMe-таблицы услуг: создан `tmp/pdfs/pdfme-manual-service-table-test.pdf` и PNG-рендер `tmp/pdfs/pdfme-manual-service-table-test-page1.png`; визуально проверено, что заголовок и строка услуги больше не пересекаются с границами таблицы.
- [x] `npm run lint` после перевода PDFMe-таблицы услуг на ручную разметку.
- [x] `npm run build` после перевода PDFMe-таблицы услуг на ручную разметку.
- [x] `npm run test:local-api` после перевода PDFMe-таблицы услуг на ручную разметку.
- [x] `npm run lint` после подключения `Multi-Variable Text` в PDFMe Designer.
- [x] `npm run build` после подключения `Multi-Variable Text` в PDFMe Designer.
- [x] `npm run lint` после добавления расширенных PDFMe-схем и вкладки `Блоки`.
- [x] `npm run build` после добавления расширенных PDFMe-схем и вкладки `Блоки`.
- [x] `npm run test:local-api` после добавления расширенных PDFMe-схем и вкладки `Блоки`.
- [x] Smoke-тест PDFMe расширенных схем: создан `tmp/pdfs/pdfme-expanded-plugins-smoke.pdf`; Multi-Variable Text подставляет кириллицу, пустой image-плейсхолдер не ломает генерацию.

## Аудит и исправления 2026-04-20

### Выполнено

- [x] Исправлен поиск клиентов: теперь поиск проходит по ФИО, телефону, email, паспортным данным, адресу и дополнительным полям; для юрлиц также по ИНН, КПП, ОГРН, реквизитам и контактному лицу.
- [x] Исправлен поиск договоров: теперь договоры ищутся не только по номеру и имени клиента, но и по телефону, email, паспортным данным и реквизитам связанного клиента.
- [x] Добавлена строгая проверка дат и времени предброни: пустое, некорректное или слишком короткое бронирование больше не сохраняется; для коттеджей проверяется минимум 1 ночь по календарным датам.
- [x] Исправлен риск двойного сохранения клиента: submit-кнопка в модалке клиента больше не вызывает `handleSubmit` вторым обработчиком.
- [x] Резервное копирование SQLite переведено с обычного `copyFile` на штатный SQLite backup API `better-sqlite3`, чтобы копия была консистентной при WAL-журнале.
- [x] PDFMe больше не заменяет пользовательскую `service_table` на ручную таблицу во время нормализации шаблона.
- [x] В PDFMe Designer разрешено добавлять одну и ту же переменную несколько раз: повторные поля получают уникальное имя, а исходная CRM-переменная хранится в `crmVariable`.
- [x] Генератор и предпросмотр PDFMe заполняют повторные поля по `crmVariable`, поэтому дубли переменных получают те же реальные данные.
- [x] Тестовое SMTP-письмо теперь прикладывает настоящий минимальный PDF, а не текстовую строку с расширением `.pdf`.
- [x] Обработка ошибок отправки email стала устойчивой к не-JSON ответам сервера.
- [x] Добавлен отдельный проверочный скрипт `npm run test:audit-fixes` для исправлений поиска, дат, PDFMe, email-ошибок и SQLite backup.

### Проверки

- [x] `npm run test:audit-fixes`
- [x] `npm run lint`
- [x] `npm run build`

## PDFMe-договор Чунга-Чанга 2026-04-21

### Выполнено

- [x] `cc_contract_pdfme` переведен в пакетную модель: 4 страницы договора/допсоглашения Чунга-Чанга + страница счета + страница акта.
- [x] Для пакета ЧЧ страницы счета и акта подмешиваются из текущего `invoice_pdfme`, чтобы сохранить ручную PDFMe-верстку счета/акта.
- [x] В пакете ЧЧ в переменную `contract_number` передается номерная часть договора без повторного `ЧЧ`, а заголовки формируются как `ДОГОВОР №ЧЧ...`, `Счёт №ЧЧ...`, `Акт №ЧЧ...`.
- [x] Команда генерации в карточке договора переименована в `PDFMe: Пакет ЧЧ`; для Чунга-Чанги отдельная команда `PDFMe: Счёт` больше не показывается.
- [x] Редактор PDFMe открывает `cc_contract_pdfme` как единый 6-страничный пакет.
- [x] Форматирование первых страниц договора ЧЧ подтянуто под эталон `договор чч (2).pdf`: расширены текстовые поля, убраны лишние ручные переносы внутри абзацев, сохранены переносы между пунктами и уплотнены разделы 2-4.
- [x] Добавлен отдельный PDFMe-шаблон `cc_contract_pdfme` для договора Чунга-Чанга.
- [x] Шаблон сделан как единый 4-страничный документ: 2 страницы договора и 2 страницы дополнительного соглашения.
- [x] Для шаблона добавлены переменные по номеру договора, датам, ФИО клиента, подписи клиента, коттеджу, суммам, паспортным данным и реквизитам исполнителя.
- [x] В генератор PDFMe добавлен отдельный путь `generatePdfmeContractBlob(...)` для формирования PDF-договора Чунга-Чанга.
- [x] В реестр PDFMe-шаблонов добавлен новый тип документа `PDFMe / Договор Чунга-Чанга`.
- [x] PDFMe-редактор в настройках теперь умеет открывать и сохранять шаблон `cc_contract_pdfme`.
- [x] В карточке договора добавлена отдельная команда генерации `PDFMe: Договор ЧЧ` для базы Чунга-Чанга.
- [x] Добавлены TDD-проверки на 4 страницы шаблона, ключевые поля страниц и маппинг данных договора в PDFMe-вход.
- [x] Создан контрольный PDF нового шаблона: `tmp/pdfs/cc-contract-pdfme-smoke-final.pdf`.
- [x] Для контрольного PDF сохранены PNG-рендеры страниц: `tmp/pdfs/cc-contract-pdfme-smoke-final-pages/page-1.png` ... `page-4.png`.

### Проверки

- [x] `npm run test:audit-fixes`
- [x] `npm run lint`
- [x] `npm run build`
- [x] Контрольный пакетный PDF `tmp/pdfs/cc-package-pdfme-smoke.pdf` успешно собран на 6 страниц.
- [x] PNG-рендеры текущего пакета обновлены для визуальной проверки: `tmp/pdfs/current-cc-package-page-1.png` ... `page-4.png`.
- [x] Контрольный PDF `tmp/pdfs/cc-contract-pdfme-smoke-final.pdf` успешно собран на 4 страницы.
- [x] Извлечением текста подтверждено наличие ключевых маркеров: `ДОГОВОР`, `Дополнительное соглашение`, `Великая Виктория Алексеевна`, `ЧЧ216067`.

### Заметки

- [ ] Следующим этапом открыть новый шаблон в самом интерфейсе CRM и вручную довести позиционирование полей по образцу пользователя, если потребуется более точное совпадение с PDF-эталоном.
- [ ] `npm run test:local-api` не запускался в этом этапе, потому что CRM-сервер на `localhost:3000` не был запущен.

## PDFMe-шаблоны 2026-04-20

### Выполнено

- [x] Начата проработка PDFMe-шаблонов как отдельного направления: текущий редактор больше не привязан жестко только к одному внутреннему ID.
- [x] Добавлен реестр PDFMe-шаблонов `PDFME_TEMPLATE_DEFINITIONS`; сейчас в нем зарегистрирован стабильный шаблон счета `invoice_pdfme`, но структура готова к добавлению акта, счета+акта и договоров.
- [x] `PdfmeTemplateEditorModal` принимает `templateId`, `templateTitle` и `templateFileName`, поэтому один и тот же Designer можно переиспользовать для разных PDFMe-макетов.
- [x] Раздел настроек PDFMe теперь строит карточки из общего реестра шаблонов, а не из одиночной захардкоженной карточки счета.
- [x] Генератор PDFMe-счета сохранен совместимым: формирование `PDFMe: Счет` продолжает использовать `invoice_pdfme`, но загрузка и создание базового шаблона вынесены в общий путь.
- [x] Добавлена тестовая проверка стабильного PDFMe-ID и уникальности ID в реестре шаблонов.
- [x] PDFMe-шаблон счета переделан под присланный образец: банковская шапка, поставщик, директор, бухгалтер и подписи сделаны статичными, а номер счета, покупатель, услуга и суммы остаются динамическими.
- [x] Первичная правка расчета `vat_amount` для PDFMe-счета пересмотрена после уточнения: итоговая сумма должна включать НДС, поэтому актуальная формула зафиксирована ниже как `total * 5 / 105`.
- [x] Сохраненный SQLite-шаблон `invoice_pdfme` обновлен новым макетом; перед заменой создана резервная копия `data/backups/before-pdfme-invoice-layout-2026-04-20-21-11.sqlite`.
- [x] Создан и визуально проверен smoke PDF `tmp/pdfs/pdfme-invoice-target.pdf`; PNG-рендер страницы сохранен в `tmp/pdfs/pdfme-invoice-target-page1.png`.
- [x] В `public/pdfme-assets` добавлены реальные PNG без фона: `stamp.png` и `signature.png`; они используются в PDFMe-счете как статичные печать и подписи.
- [x] Исправлена логика НДС для PDFMe-счета: итоговая сумма считается уже включающей НДС 5%, а `vat_amount` выводится как включенный НДС по формуле `total * 5 / 105`.
- [x] В PDFMe-счете закреплены три итоговые строки: `Итог:`, `С учетом (НДС) 5%:`, `Всего к оплате:`.
- [x] Сохраненный SQLite-шаблон `invoice_pdfme` повторно обновлен после добавления печати/подписи и исправления НДС; перед заменой создана резервная копия `data/backups/before-pdfme-stamp-signature-vat-2026-04-20-21-19.sqlite`.
- [x] Создан и визуально проверен smoke PDF `tmp/pdfs/pdfme-invoice-assets-test.pdf`; PNG-рендер страницы сохранен в `tmp/pdfs/pdfme-invoice-assets-test-page1.png`.
- [x] Исправлены битые русские строки в интерфейсе PDFMe-редактора и нескольких формах, где текст отображался как `Рџ...`/`СЃ...`.
- [x] Проверено отсутствие типичных mojibake-маркеров в `src` после исправления кодировки.
- [x] Дополнительно исправлены оставшиеся строки вида `РРјСЏ`, `РїСЂРё`, `РґР»СЏ`, `РєРЅРѕРїРєСѓ` в настройках email, клиентах, предброни и PDFMe-редакторе.
- [x] PDFMe-счет перестроен под поля A4: левый отступ `20 мм`, правый отступ `15 мм`, рабочая ширина `175 мм`; банковская шапка, таблица услуг, итоговые строки и нижняя линия вписаны в эту ширину.
- [x] Сохраненный SQLite-шаблон `invoice_pdfme` обновлен новой геометрией; перед заменой созданы резервные копии `data/backups/before-pdfme-invoice-margins-2026-04-20-22-13-39.sqlite` и `data/backups/templates/invoice_pdfme-before-margins-2026-04-20-22-13-39.pdfme.json`.
- [x] Создан контрольный PDF `tmp/pdfs/pdfme-invoice-margins-test.pdf` и PNG-рендер `tmp/pdfs/pdfme-invoice-margins-test-page1.png`; визуально проверено, что таблица начинается на `20 мм` и заканчивается на `195 мм`.
- [x] Пользовательская ручная разметка PDFMe-счета из SQLite зафиксирована как эталон для дефолтного шаблона: выравнивание банковской таблицы, дополнительные линии ИНН/КПП, позиции подписей и печати перенесены в `createDefaultInvoicePdfmeTemplate`.
- [x] Перед фиксацией ручной разметки созданы резервные копии `data/backups/before-promote-pdfme-user-reference-2026-04-20-22-30-02.sqlite` и `data/backups/templates/invoice_pdfme-user-reference-2026-04-20-22-30-02.pdfme.json`.
- [x] Создан контрольный PDF дефолтного шаблона после переноса ручной разметки: `tmp/pdfs/pdfme-invoice-promoted-default-test.pdf`, PNG-рендер `tmp/pdfs/pdfme-invoice-promoted-default-test-page1.png`.
- [x] Исправлен формат итоговых строк PDFMe-счета: `qty_words` выводит сумму без копеек (`6 000 руб.`), а `total_words_only` выводит полную пропись с рублями (`Шесть тысяч рублей 00 копеек`).
- [x] Создан контрольный PDF для суммы `6 000`: `tmp/pdfs/pdfme-invoice-6000-words-test.pdf`, PNG-рендер `tmp/pdfs/pdfme-invoice-6000-words-test-page1.png`; текст в PDF проверен извлечением.

### Нужно сделать

- [ ] Добавить отдельные PDFMe-шаблоны для акта и для единого документа счет+акт.
- [ ] Решить, какие документы переводим в PDFMe первыми: только счет/акт или также договоры.
- [ ] После появления второго PDFMe-шаблона добавить проверку генерации по каждому типу документа.

### Проверки

- [x] `npm run test:audit-fixes`
- [x] `npm run lint`
- [x] `npm run build`
